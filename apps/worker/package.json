{
  "name": "@trading-automation/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "open-job": "tsx src/index.ts",
    "prebuild": "pnpm --filter @trading-automation/shared run prisma:generate && pnpm --filter @trading-automation/shared run build",
    "build": "tsc --build tsconfig.build.json",
    "lint": "eslint src --ext .ts",
    "test": "vitest run --config ../../vitest.config.ts"
  },
  "dependencies": {
    "@trading-automation/shared": "workspace:*"
  },
  "devDependencies": {
    "tsx": "^4.7.1",
    "typescript": "^5.4.2"
  }
}
